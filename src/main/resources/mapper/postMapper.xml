<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.candm.mapper.PostMapper">

    <select id="selectAll">
        select distinct
            m.member_name,
            p.id, p.post_title, p.post_content, p.post_view_count, p.member_id, p.created_datetime, p.updated_datetime, p.post_status
        from tbl_member m join tbl_post p
        on m.id = p.member_id
        where m.id = #{id}
        limit #{criteria.count} offset #{criteria.offset}
    </select>

    <select id="selectById">
        select distinct
            m.member_name,
            p.id, p.post_title, p.post_content, p.post_view_count, p.member_id, p.created_datetime, p.updated_datetime, p.post_status
        from tbl_member m join tbl_post p
        on m.id = p.member_id
        where p.id = #{id}
    </select>

    <select id="selectTotal">
        select count(*)
        from tbl_member m join tbl_post p
        on m.id = p.member_id
        where m.id = #{id}
    </select>

</mapper>